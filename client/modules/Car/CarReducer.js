// Import Actions
import {  } from './CarActions';

// Initial State
const initialState = {};

const CarReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default CarReducer;
